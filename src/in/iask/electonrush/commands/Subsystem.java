package in.iask.electonrush.commands;

/**
 * subsystems are resources the commands may require to run. Only one command that require a subsystem can run at a time.
 * Never will two command that requires the same subsystem be allowed to run at the same time.
 * @author ElectricFish
 *
 */

public abstract class Subsystem {

}
